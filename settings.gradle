pluginManagement {
    repositories {
        gradlePluginPortal()           // wichtig! zuerst
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
        maven { url 'https://repo.spring.io/snapshot' }   // nötig für SNAPSHOT
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
        maven { url 'https://repo.spring.io/snapshot' }
    }
}

rootProject.name = 'simple-blockchain'
include 'blockchain-core', 'blockchain-node', 'ui'

